# SaaS Product Analytics Platform

## Objective

Build an end-to-end AWS Data Engineering pipeline that simulates user activity from a SaaS application. The pipeline automatically generates product usage events, stores them in a data lake, transforms them into optimized Parquet format, catalogs the data using AWS Glue, orchestrates the workflow with Apache Airflow (MWAA), and enables analytical querying through Amazon Athena.

